# These are already injected by docker-compose.yml when you run `docker compose up`.
# This file is only a reference for running the backend manually outside Docker.

DATABASE_URL=postgresql://ksp_user:ksp_pass@localhost:5432/ksp_crime
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.1:8b
